# スライドスタイルガイド

## スライドを書く前に必ず確認すること

スライドのスタイルはプロジェクトによって異なります。**作成・大幅編集の前に、以下を人間に確認してください。**

1. **情報量**: 1枚に情報を詰め込む資料型か、キーワードだけ置いて発表者がしゃべる発表型か
2. **画像**: 図や画面キャプチャを入れたい箇所があるか。ある場合、画像はすでに手元にあるか、それとも後で用意するか
3. **トーン**: 硬め（学術・ビジネス）か、柔らかめ（社内共有・布教）か

## project.json の `style` フィールド

プロジェクトの `project.json` に `style` フィールドがある場合は、そこにスタイルの前提が書かれています。
これを読めば確認の手間を省ける場合があります。ただし、書かれていない項目は引き続き確認すること。

```json
{
  "theme": "light",
  "style": "発表型。1枚1メッセージ。画像・デモを多用。聴衆はVimを知らない一般エンジニア。"
}
```

## 画像プレースホルダー

画像を入れたい箇所で実ファイルが未確定の場合は `src: "_placeholder"` を使う。
破線枠 + `caption` でどんな画像が必要かを明示できる。

```json
{ "id": "c-img", "type": "image", "props": { "src": "_placeholder", "caption": "ここに Neovim の画面スクリーンショットを入れる", "width": 900, "height": 400 } }
```
